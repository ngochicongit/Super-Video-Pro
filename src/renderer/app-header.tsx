import {t} from "./i18n";
export function AppHeader({activeTasks,taskProgress,onTasks,onSettings}:{activeTasks:number;taskProgress:number;onTasks:()=>void;onSettings:()=>void}){const percent=Math.round(Math.max(0,Math.min(1,taskProgress))*100);return <header className="hero"><div><span className="eyebrow">{t("tagline")}</span><h1>Super Video <b>Pro</b></h1></div><div className="shell-actions"><button className={activeTasks?"has-progress":""} data-surface="tasks" onClick={onTasks}><span>◉</span>Tác vụ{activeTasks>0&&<><b>{activeTasks}</b><small>{percent}%</small><i className="task-global-progress" style={{width:`${percent}%`}}/></>}</button><button data-surface="settings" aria-label="Mở cài đặt" title="Cài đặt" onClick={onSettings}>⚙</button></div></header>}
