import {useState} from "react";
const views=["Projects","Create Project","Article","Facts","Script","Storyboard","Scene Editor","Preview","QA","Settings","Services"];
export function WebStudio(){const [view,setView]=useState("Projects");return <section className="web-studio"><nav aria-label="Studio views">{views.map(item=><button key={item} className={view===item?"active":""} onClick={()=>setView(item)}>{item}</button>)}</nav><div className="studio-content"><h2>{view}</h2><p>Studio pipeline workspace</p>{view==="Scene Editor"&&<div className="studio-actions"><textarea aria-label="Narration" placeholder="Narration"/><input aria-label="Fact references" placeholder="Fact refs"/><input aria-label="Visual prompt" placeholder="Visual prompt"/><select aria-label="Visual type"><option>article-image</option><option>kinetic-text</option><option>chart</option></select>{["Regenerate TTS","Regenerate visual","Render scene","Validate","Preview","Final render"].map(a=><button key={a}>{a}</button>)}</div>}</div></section>}
