import {useRef} from "react";
import {t} from "./i18n";
export type AppTab="download"|"composition"|"history"|"studio";
const tabs:AppTab[]=["download","composition","studio","history"];
export function AppTabs({active,onChange}:{active:AppTab;onChange:(tab:AppTab)=>void}){const refs=useRef<Array<HTMLButtonElement|null>>([]);function navigate(event:React.KeyboardEvent,index:number){if(!["ArrowLeft","ArrowRight","Home","End"].includes(event.key))return;event.preventDefault();const next=event.key==="Home"?0:event.key==="End"?tabs.length-1:(index+(event.key==="ArrowRight"?1:-1)+tabs.length)%tabs.length;onChange(tabs[next]!);requestAnimationFrame(()=>refs.current[next]?.focus());}return <nav className="app-tabs" aria-label={t("navigation")} role="tablist">{tabs.map((tab,index)=><button ref={element=>{refs.current[index]=element;}} key={tab} id={`tab-${tab}`} data-tab={tab} role="tab" aria-selected={active===tab} tabIndex={active===tab?0:-1} className={active===tab?"active":""} onKeyDown={event=>navigate(event,index)} onClick={()=>onChange(tab)}>{tab==="studio"?"AI News Studio":t((`tab_${tab}`) as "tab_download")}</button>)}</nav>}
