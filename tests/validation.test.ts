import { describe,expect,it } from "vitest";import fs from "node:fs/promises";import os from "node:os";import path from "node:path";import {validateFinal} from "../src/main/validation";
describe("final validation",()=>{it("rejects missing and empty outputs",async()=>{const dir=await fs.mkdtemp(path.join(os.tmpdir(),"svp-validation-"));expect((await validateFinal(path.join(dir,"missing.mp4"))).valid).toBe(false);const empty=path.join(dir,"empty.mp4");await fs.writeFile(empty,"");const result=await validateFinal(empty);expect(result.valid).toBe(false);expect(result.reasons).toContain("Final file is empty");await fs.rm(dir,{recursive:true,force:true})})});
